# Observability system dependencies for Ordinaut

# Prometheus metrics collection
prometheus_client>=0.17.0

# Structured logging (already available in Python standard library)
# json, logging, uuid, datetime, contextvars are built-in

# Health monitoring dependencies
# asyncio (built-in)
psycopg[binary]>=3.1.19  # PostgreSQL async driver for health checks
redis>=5.0.0             # Redis client for health checks

# Alert system dependencies  
# sqlalchemy (already required by main system)
# os, time, datetime, enum, dataclasses, typing (built-in)

# Optional: Enhanced logging
structlog>=23.0.0        # Optional structured logging enhancement

# Optional: Distributed tracing
opentelemetry-api>=1.26.0           # OpenTelemetry API
opentelemetry-sdk>=1.26.0           # OpenTelemetry SDK  
opentelemetry-instrumentation>=0.47b0  # Auto-instrumentation